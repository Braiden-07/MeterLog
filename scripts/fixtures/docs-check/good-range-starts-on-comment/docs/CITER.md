# A range that legitimately STARTS on a comment line

Only BLANK is refused as a range start. A comment or a prose line is an ordinary
and correct place for a cited span to begin.

See [`a revoked person can be re-invited to the same tenant`](../src/spec.ts#L3-L9).
