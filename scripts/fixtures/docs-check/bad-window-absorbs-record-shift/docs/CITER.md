# Mechanism (1): the proximity window absorbs a heading shift

The anchor is NOT blank, so the blank-anchor rule cannot see it. It points two
lines above the heading it names, and the old +/-5 window found the heading
anyway. This fixture is caught ONLY by the record-id exactness rule.

See [ADR-004](REGISTER.md#L4) — that line is prose; the heading is at L6.
